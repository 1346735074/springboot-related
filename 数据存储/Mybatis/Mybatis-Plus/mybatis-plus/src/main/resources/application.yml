# DataSource Config
server:
  port:8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver #新的mysql驱动
    url: jdbc:mysql://118.25.93.64:3306/user?useUnicode=true&useSSL=false&characterEncoding=utf8
    username: root
    password: 123123
# Logger Config
logging:
  level:
    com.baomidou.mybatisplus.samples.pagination: debug
mybatis-plus:
#  #基本配置
  mapper-locations: classpath:mapper/*Mapper.xml #Mapper 中有自定义方法（XML 中有自定义实现）
  type-aliases-package: com.demo.ant.entity #实体类扫描地址
  type-enums-package: com.demo.ant.entity.enums #枚举类 扫描路径
#  config-location: classpath:mybatis-config.xml #MyBatis 配置文件位置
#  type-handlers-package: com.baomidou.ant.user.handler #handler扫描地址
#  check-config-location: false #启动时是否检查 MyBatis XML 文件的存在 默认false
#  executor-type: simple #执行器
#  configuration-properties: classpath:mybatis/config.properties #指定外部化
#  global-config:
#    sql-session: com.xxx.SqlSession #默认为空
#  sql-session-factory: org.apache.ibatis.session.SqlSessionFactory #默认为空

